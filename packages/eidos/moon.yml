tasks:
  test:
    command: 'bun test'
    deps:
      - 'idion:build'
      - 'zygon:build'
      - 'ptoma:build'
  lint:
    command: 'biome run lint'
    deps:
      - 'idion:build'
      - 'zygon:build'
      - 'ptoma:build'
  build:
    command: 'bun run dist'
    deps:
      - 'idion:build'
      - 'zygon:build'
      - 'ptoma:build'
