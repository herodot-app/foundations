tasks:
  test:
    command: 'bun test'
    deps:
      - 'idion:build'
  lint:
    command: 'bun run lint'
    deps:
      - 'idion:build'
  build:
    command: 'bun run dist'
    deps:
      - 'idion:build'
