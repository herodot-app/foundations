tasks:
  test:
    command: 'bun test'
    deps:
      - 'idion:build'
      - 'zygon:build'
  lint:
    command: 'bun run lint'
    deps:
      - 'idion:build'
      - 'zygon:build'
  build:
    command: 'bun run dist'
    deps:
      - 'idion:build'
      - 'zygon:build'
